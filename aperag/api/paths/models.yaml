models:
  get:
    summary: Get models
    description: Get models
    responses:
      '200':
        description: Models
        content:
          application/json:
            schema:
              $ref: '../components/schemas/model.yaml#/modelList'
      '401':
        description: Unauthorized
        content:
          application/json:
            schema:
              $ref: '../components/schemas/common.yaml#/failResponse'