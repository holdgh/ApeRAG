apiKey:
  type: object
  properties:
    id:
      type: string
    key:
      type: string

apiKeyList:
  type: object
  description: A list of API keys
  properties:
    items:
      type: array
      items:
        $ref: '#/apiKey'
    pageResult:
      $ref: './common.yaml#/pageResult'