// import { Guard } from '@authing/guard-react18';
// import { env } from '@/models/system';

// let authingClient: Guard | undefined;

// if (env.auth.type === 'authing') {
//   authingClient = new Guard({
//     appId: env.auth.auth_app_id,
//     redirectUri: window.location.origin + '/callback',
//   });
// }

// export default authingClient;
