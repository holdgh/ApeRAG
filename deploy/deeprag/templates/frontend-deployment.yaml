apiVersion: apps/v1
kind: Deployment
metadata:
  name: deeprag-website
  labels:
    app: deeprag-website
spec:
  replicas: 1
  selector:
    matchLabels:
      app: deeprag-website
  template:
    metadata:
      labels:
        app: deeprag-website
    spec:
      containers:
      - image: {{ .Values.image.website.registry | default "docker.io" }}/{{ .Values.image.website.repository }}:{{ .Values.image.website.tag }}
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        name: deeprag-website
        ports:
        - containerPort: 80
          protocol: TCP
      imagePullSecrets:
      - name: docker-hub-secret

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: deeprag-console
  labels:
    app: deeprag-console
spec:
  replicas: 1
  selector:
    matchLabels:
      app: deeprag-console
  template:
    metadata:
      labels:
        app: deeprag-console
    spec:
      containers:
      - image: {{ .Values.image.console.registry | default "docker.io" }}/{{ .Values.image.console.repository }}:{{ .Values.image.console.tag | default .Chart.Version }}
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        name: deeprag-console
        ports:
        - containerPort: 80
          protocol: TCP

