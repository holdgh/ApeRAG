apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "llmserver.fullname" . }}-test-connection"
  labels:
    {{- include "llmserver.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "llmserver.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
